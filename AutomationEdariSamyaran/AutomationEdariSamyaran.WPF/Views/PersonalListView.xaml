<UserControl x:Class="AutomationEdariSamyaran.WPF.Views.PersonalListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:md="clr-namespace:MaterialDesignThemes.Wpf;assembly=MaterialDesignThemes.Wpf"
             mc:Ignorable="d"
             FlowDirection="RightToLeft"
             FontFamily="Tahoma">

    <DockPanel Margin="10">

        <!-- بخش جستجو -->
        

        <!-- دکمه‌ها -->
        <StackPanel Orientation="Horizontal" DockPanel.Dock="Top" HorizontalAlignment="Right" Margin="0,0,0,10">
            <TextBox Width="200" Margin="5"  
          Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" />
            <Button Content="جستجو" Command="{Binding SearchCommand}" Style="{StaticResource MaterialDesignRaisedButton}" Margin="5"/>
            <Button Content="جدید" Command="{Binding AddCommand}" Style="{StaticResource MaterialDesignRaisedButton}" Margin="5"/>
            <Button Content="ویرایش" Command="{Binding EditCommand}" Style="{StaticResource MaterialDesignOutlinedButton}" Margin="5"/>
        </StackPanel>

        <!-- DataGrid -->
        <DataGrid ItemsSource="{Binding FilteredPersonals}" SelectedItem="{Binding SelectedPersonal}" AutoGenerateColumns="False"
                  CanUserAddRows="False" CanUserDeleteRows="False"
                
                  GridLinesVisibility="Horizontal" Margin="0,0,0,0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="نام کامل" Binding="{Binding FullName}" Width="*" />
                <DataGridTextColumn Header="کد پرسنلی" Binding="{Binding PersonalCode}" Width="*" />
                <DataGridTextColumn Header="واحد / دپارتمان" Binding="{Binding Department.Name}" Width="*" />
                <DataGridTextColumn Header="پست" Binding="{Binding Position.Name}" Width="*" />
                <DataGridTextColumn Header="ایمیل" Binding="{Binding Email}" Width="*" />
                <DataGridTextColumn Header="تلفن" Binding="{Binding Phone}" Width="*" />
                <DataGridCheckBoxColumn Header="فعال" Binding="{Binding IsActive}" Width="Auto" />
            </DataGrid.Columns>
        </DataGrid>

    </DockPanel>
</UserControl>
