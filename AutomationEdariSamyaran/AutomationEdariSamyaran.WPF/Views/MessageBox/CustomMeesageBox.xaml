<Window x:Class="AutomationEdariSamyaran.WPF.Views.CustomMessageBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AutomationEdariSamyaran.WPF.Views"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d" 
        Title="LogIn" Height="240" Width="350" WindowStyle="None" WindowStartupLocation="CenterScreen"

        xmlns:wpf="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        FontFamily="{StaticResource MyFontName}" FlowDirection="RightToLeft" 
        TextElement.FontWeight="Bold"
        TextElement.FontSize="14"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        Style="{StaticResource MaterialDesignWindow}" Loaded="CustomMeesageBox_OnLoaded">


    <materialDesign:Card Margin="5"  Background="WhiteSmoke" UniformCornerRadius="5" wpf:NavigationRailAssist.CornerRadius="5">


        <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
               <Border HorizontalAlignment="Center">
                   <Label   Margin="2" Name="lblMessage" FontWeight="Bold">اخطار</Label></Border>

            <TextBox Grid.Row="1" Name="TxtMessage" Height="130" Margin="2" BorderBrush="Red" BorderThickness="2"
                             FlowDirection="RightToLeft" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" TextWrapping="Wrap" Background="{DynamicResource {x:Static SystemColors.MenuBrushKey}}"></TextBox>
                <DockPanel Grid.Row="2" Height="40" Margin="2" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <Button Margin="5"   Height="25" Width="100" Click="ButtonBase_OnClick" Background="#FFB6FCFF">
                        <DockPanel>
                            <materialDesign:PackIcon Kind="OkRu" Width="15" Height="15" Foreground="Green"/>
                            <TextBlock Text="تایید"></TextBlock>
                        </DockPanel>
                    </Button>

                </DockPanel>



            </Grid>
    </materialDesign:Card>
</Window>
