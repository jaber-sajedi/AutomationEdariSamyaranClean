<Window x:Class="AutomationEdariSamyaran.WPF.Views.QuestionMeesageBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AutomationEdariSamyaran.WPF.Views"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d" 
        Title="LogIn" Height="200" Width="350" WindowStyle="None" WindowStartupLocation="CenterScreen"

        xmlns:wpf="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}"
        Style="{StaticResource MaterialDesignWindow}" FlowDirection="RightToLeft" Loaded="QuestionMeesageBox_OnLoaded"
       >


    <materialDesign:Card Margin="5"  Background="WhiteSmoke" UniformCornerRadius="5" wpf:NavigationRailAssist.CornerRadius="5">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBox Name="txtMessage" Height="130" Margin="5" BorderBrush="Yellow" BorderThickness="2" FlowDirection="RightToLeft" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" TextWrapping="Wrap" Background="{DynamicResource {x:Static SystemColors.MenuBrushKey}}"></TextBox>
            <DockPanel Grid.Row="1" Height="40" Margin="5 0 5 5" HorizontalAlignment="Center" VerticalAlignment="Center">
                <Button  Name="txtCancel" Click="TxtCancel_OnClick" Margin="5" Background="#FFF3C9C9"   Height="25" Width="90"  >
                    <DockPanel>
                        <materialDesign:PackIcon Kind="ExitRun" Width="15" Height="15" Foreground="Red"/>
                        <TextBlock Text="انصراف"></TextBlock>
                    </DockPanel>
                </Button>
                <Button Grid.Row="2" Margin="5" Background="#FFB6FCFF"   Height="25" Width="90" Name="txtOk" Click="TxtOk_OnClick">
                    <DockPanel>
                        <materialDesign:PackIcon Kind="OkRu" Width="15" Height="15" Foreground="Green"/>
                        <TextBlock Text="تایید"></TextBlock>
                    </DockPanel>
                </Button>

            </DockPanel>





        </Grid>
        </materialDesign:Card> 
</Window>
